apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskflow-frontend
  namespace: #{Namespace}
spec:
  replicas: #{ReplicaCount}
  selector:
    matchLabels:
      app: taskflow-frontend
  template:
    metadata:
      labels:
        app: taskflow-frontend
    spec:
      containers:
      - name: taskflow-frontend
        image: mattallford/taskflow-web:#{ImageTag}
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: taskflow-frontend-service
  namespace: #{Namespace}
spec:
  selector:
    app: taskflow-frontend
  ports:
  - port: 3000
    targetPort: 3000
  type: ClusterIP