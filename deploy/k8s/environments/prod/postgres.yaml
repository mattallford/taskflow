apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskflow-postgres-prod
  namespace: taskflow-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taskflow-postgres-prod
  template:
    metadata:
      labels:
        app: taskflow-postgres-prod
    spec:
      containers:
      - name: postgres
        image: postgres:15
        imagePullPolicy: Always
        env:
        - name: POSTGRES_DB
          value: "taskflowdb-prod"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: taskflow-secrets-prod
              key: postgres-username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: taskflow-secrets-prod
              key: postgres-password
        ports:
        - containerPort: 5432
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          exec:
            command:
              - pg_isready
              - -U
              - taskflow
              - -d
              - taskflowdb-prod
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          exec:
            command:
              - pg_isready
              - -U
              - taskflow
              - -d
              - taskflowdb-prod
          initialDelaySeconds: 10
          periodSeconds: 15
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-storage
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: taskflow-postgres-prod
  namespace: taskflow-prod
spec:
  selector:
    app: taskflow-postgres-prod
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP