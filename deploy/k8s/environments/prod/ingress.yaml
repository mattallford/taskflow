apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: taskflow-ingress-prod
  namespace: taskflow-prod
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: taskflow.local
    http:
      paths:
      # API routes
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: taskflow-api-prod
            port:
              number: 8080
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: taskflow-api-prod
            port:
              number: 8080
      - path: /swagger
        pathType: Prefix
        backend:
          service:
            name: taskflow-api-prod
            port:
              number: 8080
      # Frontend routes (catch-all)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: taskflow-frontend-prod
            port:
              number: 3000